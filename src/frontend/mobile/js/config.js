(function () {
    window.MobileConfig = Object.freeze({
        breakpoint: 850,
        modeTouchDebounceMs: 700,
        modeSelectionDebounceMs: 250,
        swipeThreshold: 50,
        longPressMs: 320
    });
})();
