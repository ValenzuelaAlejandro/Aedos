{
  "name": "aedos",
  "version": "1.0.0",
  "description": "Local web application that allows students to automatically generate academic presentations in PDF using the Google Gemini API.",
  "main": "src/backend/server.js",
  "scripts": {
    "start": "node src/backend/server.js",
    "dev": "node --watch src/backend/server.js",
    "build": "npx puppeteer install",
    "postinstall": "rm -rf .cache/puppeteer && npx puppeteer install"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "express": "^4.22.1",
    "motion": "^12.35.1",
    "puppeteer": "^24.39.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "yauzl": "^3.2.1"
  }
}
